// This is read in once through Cypress
// The plugins file is a special file that executes in Node before the project is loaded,
// before the browser launches, and during your test execution.

export default (on, config) => {
  // `on` is used to hook into various events Cypress emits
  // `config` is the resolved Cypress config
};
